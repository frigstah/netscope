{
  "schemaVersion": 1,
  "id": "io.github.frigstah.netscope",
  "name": "NetScope",
  "version": "1.6.0",
  "author": "frig",
  "license": "MIT",
  "description": "LAN reconnaissance for Omarchy: local IPs per interface, public IP, every unit on the network and a port probe for any of them. Developed for and by frig.",
  "kinds": [
    "bar-widget"
  ],
  "entryPoints": {
    "barWidget": "Panel.qml"
  },
  "barWidget": {
    "displayName": "NetScope",
    "description": "Interfaces, public IP and last LAN sweep at a glance; opens the NetScope window.",
    "category": "Network",
    "allowMultiple": false,
    "defaultSection": "right",
    "defaults": {
      "pollSeconds": 30,
      "autoScanOnOpen": false
    },
    "schema": [
      {
        "key": "pollSeconds",
        "type": "integer",
        "label": "Status poll (seconds)",
        "min": 5,
        "max": 600,
        "step": 5,
        "defaultValue": 30,
        "description": "How often the popout refreshes interfaces and the cached public address while open."
      },
      {
        "key": "autoScanOnOpen",
        "type": "boolean",
        "label": "Sweep on open",
        "defaultValue": false,
        "description": "Run a LAN sweep in the background every time the popout opens."
      }
    ]
  },
  "icon": "icon.png"
}
